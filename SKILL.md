---
name: progressive-handbook
description: 把一个主题或本地知识库制作成渐进披露式交互知识手册（俗称：点读手册、P社手册）——单文件离线 HTML，含章节化导航、术语二级窗口（点词滑出解释侧板、可层层跳转）、概念图谱、术语速查表、自测与阅读进度。用户要求做知识手册、交互教程、点词学习页、把知识库/课程变成可点着学的网页，或提到布迪厄知识手册、P社手册、嵌套提示（nested tooltips）、渐进披露（progressive disclosure）式学习页时使用。不做公众号排版，不做无术语层的普通静态长文。
---

# progressive-handbook：渐进披露知识手册（点读手册）

## 模式谱系（这个形态从哪来）

点词展开解释的形态**不是任何一家游戏公司的发明**，它是三个旧事物的合流：

- **超文本联想跳转**：1945 年 Vannevar Bush 的 Memex 设想「联想索引」，1965 年 Ted Nelson 定名 hypertext——词与词互相链接、读者沿兴趣路径游走；
- **游戏内百科**：1991 年《文明》的 Civilopedia 开创了「术语词典层」这一游戏制度，比 Paradox 首款作品早九年；
- **情境提示**：1990 年代 GUI 的 tooltip，在鼠标所在处按需给解释。

Paradox（2010 年代 CK2/EU4 时期）把三者压缩进提示层、打磨成标志性的 nested tooltips 并用于机制教学，所以「P社手册」只是俗名。同时，教育技术研究（Edwards & Hardman 1989 起的 lost in hyperspace 系列）早已指出：纯非线性超文本会让读者迷航。**本格式的双结构正是文献给出的解方：线性章节主线（防迷航）+ 术语抽屉（按需展开）+ 概念图谱（结构地图）。**

命名策略：对外传播用「**点读手册**」（人人秒懂，和点读笔一个道理）或学术场合用「**渐进披露知识手册**」；「P社手册」降级为圈内别名，只当传播钩子用。

## 核心任务

把「一个主题 + 它的专业术语」加工成一本**可点着学的单文件 HTML 手册**：读者顺序读章节，遇到不懂的词点开即有解释，读完可自测。一次交付 = 一个文件夹，内含一个自包含、离线可用的 HTML。

产出路径约定：`<工作区>/<主题名>知识手册/<主题名>知识手册.html`。

## 设计不变量（为什么是「非打断式」）

五条不变量，改动前先确认读者利益：

1. **不打断阅读流**：解释永远是叠加层（右侧抽屉），关闭后无缝续读；没有弹窗、没有强制引导。
2. **信息按需出现**：章节正文只放主线；深度内容全部下沉进术语抽屉；每章标注预计时长。
3. **术语互联**：每个 CONCEPTS 术语必须写 `rel` 相关概念（3~8 个，成对书写），保证从任何术语都能「跳着学」；图谱据此建边，不许有孤岛节点。
4. **有存档点**：每章一个「标记已读」，进度持久化；读者随时可离开、可回来。
5. **离线单文件**：零外部依赖、零网络请求，双击即开。

## 输入与最少提问

必需要有一个**主题**（外加素材更佳：本地知识库文件夹、文章、讲稿均可）。读者和深度默认「零基础、按层递进」。只有当素材为空、且无法从上下文判断章节划分时，才向用户要素材或确认大纲——其余决定自行做出并在交付时说明。

## 工作流

### 1. 内容设计

读 `references/content-design.md`，产出：层级大纲（1~6 层，每层 2~3 章）、每章时长、术语清单（30~40 个，含 def/rel 分组）。术语清单是本形态的灵魂，先设计术语再写章节——章节正文里要自然地「用」到这些词，让自动链接生效。

### 2. 拼装

复制 `assets/handbook-skeleton.html` 到交付路径，替换以下区域（骨架内有对应注释与 `【】` 占位，逻辑代码一行不动）：

| 区域 | 位置 | 动作 |
| --- | --- | --- |
| 标题与描述 | `<title>`、meta、顶栏 brand | 改文字 |
| 欢迎层 | `#welcome` | 标题、导语、四张入口卡（data-go 指向真实章节） |
| 章节 | `main.wrap` 里的 section | 按大纲增删改；工具三章（graph/glossary/quiz）id 不动 |
| 概念数据 | `CONCEPTS`、`GROUP_NAMES` | 整体替换，字段规范见 content-design |
| 术语速查 | `GLOSSARY` | 整体替换 |
| 路线 / 坑 / 测验 | `ROUTES` / `MISCONCEPTIONS` / `QUIZ` | 整体替换，goto 指向真实章节 |
| 图例文字 | `#graph` 章的 legend | 按六组语义改写 |

### 3. 验证

```bash
python3 <skill目录>/scripts/verify_handbook.py <手册.html>
```

必须全绿才算完成。该脚本检查：JS 语法、章节与 CONCEPTS 交叉引用（data-c / data-go / rel / ch / goto 不得有死链）、必渲染容器存在、遗留占位符、术语数量下限。可再做 Level 2 浏览器冒烟（现成片段见 `references/verification.md`）。

### 4. 交付报告

报告：产出路径、章节数 / 术语数 / 题数、验证等级（结构校验通过 / 行为冒烟通过）、阅读建议（先读哪章）。

## 边界与停止条件

- 不覆盖已存在的交付目录；同名先询问。
- 内容忠于用户素材，不编造事实；转述外部知识时在抽屉 `src` 字段注明位置。
- 素材含未授权私密内容时拒绝写入。
- 验证不过且无法定位原因时停止并报告，不带病交付。
- 参考成品：工作区下 `GitHub实用教程/GitHub新手实用教程.html`（完整 39 术语实例）与 `布迪厄知识手册/布迪厄知识手册.html`（该形态的起点，当对照答案用）。
